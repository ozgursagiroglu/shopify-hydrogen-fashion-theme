/**
 * Custom Hooks Export
 * Centralized exports for all custom React hooks
 *
 * Usage:
 * import { useLocalStorage, useSimpleLocalStorage } from '~/lib/hooks';
 */

export {
  useLocalStorage,
  useSimpleLocalStorage,
  type UseLocalStorageOptions,
} from './useLocalStorage';
